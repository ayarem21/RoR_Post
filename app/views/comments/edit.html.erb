<h4>Edit comment:</h4>

<%= form_for([@comment]) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', posts_path %>
